version: "3"

services:
  db:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: sandbox
      MYSQL_USER: docker
      MYSQL_PASSWORD: docker
    ports:
      - 33306:3306
